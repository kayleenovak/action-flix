import { createUser } from '../createUser.js'

describe('createUser', () => {
  let mockUrl
  let mockDispatch

  beforeEach(() => {
    mockUrl = 'http://www.mocksite.com'
    mockDispatch = jest.fn()
  })
})